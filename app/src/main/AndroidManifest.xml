<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ndnu.tdy.CreazyCat" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Activity.GameActivity">

        </activity>

        <service
            android:name=".Music.MusicServer"
            android:exported="true"
            android:process=":remote" />

        <service
            android:name=".Music.MusicServer2"
            android:exported="true"
            android:process=":remote" />

        <service
            android:name=".Music.MusicServer3"
            android:exported="true"
            android:process=":remote" />

        <activity android:name=".Activity.ChooseActivity"/>
        <activity
            android:name=".Activity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>